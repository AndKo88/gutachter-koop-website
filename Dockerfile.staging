FROM node:20-alpine
WORKDIR /app
RUN npm install -g pnpm@latest --silent
COPY package.json pnpm-lock.yaml ./
RUN pnpm install --frozen-lockfile
# Source is bind-mounted at runtime (see docker-compose.staging.yml)
ENV PORT=3000
ENV NEXT_TELEMETRY_DISABLED=1
EXPOSE 3000
CMD ["pnpm", "dev"]
